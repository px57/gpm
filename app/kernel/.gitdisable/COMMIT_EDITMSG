feat: Add colored output to error messages

Why:
- Adding color to error messages can make them more noticeable and easier to identify at a glance.

Changes:
- Imported necessary modules for colored output in error messages.
- Modified the raise_error function to include color formatting for the error message.

# Veuillez saisir le message de validation pour vos modifications. Les lignes
# commençant par '#' seront ignorées, et un message vide abandonne la validation.
#
# Sur la branche main
# Votre branche est à jour avec 'origin/main'.
#
# Modifications qui seront validées :
#	modifié :         message/error.py
#
