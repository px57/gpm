chore: add docker attach functionality and docker container id

Why:

- To add functionality for attaching to a Docker container
- To add a method for retrieving the Docker container id

Changes:

- Added `docker/attach.py` to handle attaching to a container
- Import `os` and `get_container_id` function from `docker/id.py` in `docker/attach.py`
- Implemented `docker_attach` function in `docker/attach.py` for attaching to a container

# Veuillez saisir le message de validation pour vos modifications. Les lignes
# commençant par '#' seront ignorées, et un message vide abandonne la validation.
#
# Sur la branche main
# Votre branche est en avance sur 'origin/main' de 4 commits.
#   (utilisez "git push" pour publier vos commits locaux)
#
# Modifications qui seront validées :
#	nouveau fichier : docker/attach.py
#	nouveau fichier : docker/id.py
#	modifié :         docker/ps.py
#
