feat: Add docstring to django_settings_check_keys function

Why: The docstring was added to provide clarity on the purpose of the `django_settings_check_keys` function.

Changes:
- Added a docstring to the `django_settings_check_keys` function explaining its purpose.

# Veuillez saisir le message de validation pour vos modifications. Les lignes
# commençant par '#' seront ignorées, et un message vide abandonne la validation.
#
# Sur la branche main
# Votre branche est à jour avec 'origin/main'.
#
# Modifications qui seront validées :
#	modifié :         dependencies/django_settings.py
#
